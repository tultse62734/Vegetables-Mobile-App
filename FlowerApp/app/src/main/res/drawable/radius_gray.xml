<?xml version="1.0" encoding="utf-8"?>
<shape android:shape="rectangle" xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/colorGrayHint">
    </solid>
    <!-- đặt thuộc tính màu và độ rộng của đường viền -->
    <stroke android:color="@color/colorGrayHint"
        android:width="@dimen/dp2">
    </stroke>
    <!-- các thuộc tính căn chỉnh -->
    <padding android:bottom="@dimen/dp10" android:left="@dimen/dp10" android:right="@dimen/dp10" android:top="@dimen/dp10">
    </padding>
    <!-- và đây là bán kính đường tròn ở 4 góc -->
    <corners android:radius="@dimen/dp10">
    </corners>
</shape>